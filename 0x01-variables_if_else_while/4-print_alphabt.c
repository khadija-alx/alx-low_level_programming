#include <stdio.h>
/**
  *main - prints the alphabet in lowercase, followed by a new line
  *
  *Return: Always 0 (success)
  */

int main(void)
{
	char ch = 'a' && ch != 'q' && ch != 'e';

	while (ch <= 'z')
	{
		putchar(ch);
		ch++;
	}
	putchar('\n');

	return (0);
}
